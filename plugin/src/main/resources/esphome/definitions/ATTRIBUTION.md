# Attribution

The ESPHome component catalog in this directory (`components.index.json` and
`components/*.json`) is vendored from the **ESPHome Device Builder** project:

- Source: https://github.com/esphome/device-builder (`esphome_device_builder/definitions/`)
- © ESPHome contributors
- Licensed under the Apache License, Version 2.0 (see `LICENSE`).

The catalog is generated by `device-builder`'s `script/sync_components.py` from
ESPHome's published schema bundle (https://schema.esphome.io) plus light
`esphome` introspection. See `docs/esphome-data-sources.md` for details.

This spike vendors only a small subset of components for demonstration. The full
catalog, pinned to a specific ESPHome release, will be vendored (or generated at
build time) for the released plugin.
